import React from 'react';

import Login from './Login';

const pages = {
    login: {
        page: Login,
        hasHeader: false,
        hasSideMenu: false,
        className: 'fade-in',
    }
}

export default pages;